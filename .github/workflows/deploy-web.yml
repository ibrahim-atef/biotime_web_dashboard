name: Deploy Flutter Web

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build web
        env:
          API_URL: ${{ secrets.BIOTIME_API_URL }}
        run: |
          URL="${API_URL:-http://localhost:3000}"
          flutter build web --release \
            --base-href "/biotime_web_dashboard/" \
            --dart-define=BIOTIME_API_URL="$URL"

      # Puts built site in docs/ on main (matches Pages: main → /docs)
      - name: Deploy to docs folder on main
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build/web
          publish_branch: main
          destination_dir: docs
          keep_files: true
